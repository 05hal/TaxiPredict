集成学习模型适用于本项目这类结构化宽表数据，能够处理非线性关系、类别编码特征、天气变量、高峰标记和历史统计特征之间的组合影响。本项目分别实现了 LightGBM、CatBoost 和 XGBoost 三类模型，作为小时级 Geohash 需求预测的主要表格模型。

集成学习的基本思想是将多个弱学习器组合为一个更强的预测模型。以梯度提升树为例，模型按加法形式逐步构建：

$$\hat{y}_i=\sum_{k=1}^{K} f_k(\mathbf{x}_i), \quad f_k \in \mathcal{F}$$

其中，$$f_k$$ 表示第 $$k$$ 棵回归树，$$K$$ 为树的数量，$$\mathcal{F}$$ 为回归树函数空间。每一轮训练都尝试拟合前一轮模型的残差或负梯度，从而不断降低损失函数。

LightGBM 模型采用梯度提升决策树作为基础模型。特征工程在模型训练前统一由特征构建模块完成，包括区域前缀 geo_prefix、多窗口历史均值、区域平均订单量、holiday_peak、rain_peak、weekend_peak 等交互项，以及通过分位数分箱构造的 vis_level 和 wind_level 等级特征。由于 pickups 存在长尾分布，模型支持对目标值使用 log1p 变换（由配置文件开关控制），预测后再反变换回订单量。参数设置上采用较小学习率、叶子数约束、最大深度约束以及 L1/L2 正则化，以降低过拟合风险。训练完成后输出 6 月最后一周的预测值与真实值对比图，并生成 Top-N 特征重要性图及 SHAP 可解释性分析。

LightGBM、CatBoost 和 XGBoost 都属于提升树模型，但侧重点有所不同。LightGBM 更强调训练效率和高维稀疏特征处理；CatBoost 对类别特征处理更加友好；XGBoost 则在正则化目标函数和工程稳定性方面较成熟。XGBoost 的典型目标函数可写为：

$$\mathcal{Obj}=\sum_{i=1}^{N}l(y_i,\hat{y}_i)+\sum_{k=1}^{K}\Omega(f_k)$$

$$\Omega(f)=\gamma T+\frac{1}{2}\lambda\sum_{j=1}^{T}w_j^2$$

其中，$$l$$ 为预测损失，$$\Omega$$ 为树复杂度惩罚项，$$T$$ 为叶子节点数量，$$w_j$$ 为第 $$j$$ 个叶子的权重。该正则化项能够抑制树结构过于复杂，从而降低过拟合风险。

CatBoost 模型同样用于回归预测，主要补充验证类别型空间和时间特征的建模效果。项目中对 time_cat、day_cat、geo_prefix 等字段以原生类别特征形式传入，利用 CatBoost 的有序提升（Ordered Boosting）和对称树结构进行建模，并构造 peak_weekend、holiday_peak、rain_peak、temp_rhum、temp_wspd 等组合特征，使模型同时利用时间周期、空间区域、天气状态和运营规则信息。模型使用 800 次迭代、深度为 6 的树结构，并通过随机采样和正则化参数控制泛化能力。训练完成后生成 CatBoost 的预测对比图和特征重要性分析。

XGBoost 模型在项目中作为增强版实验模型，支持两种特征模式。raw_all 模式直接使用特征工程阶段的全部数值化特征；ts_topk 模式在独立特征选择模块中，先构造时序特征（包括 pickups_lag_1、pickups_lag_3、pickups_lag_6、pickups_lag_24、滚动均值、EMA 指数移动平均、差分特征和同小时历史特征），再按训练集相关性筛选 Top-K 特征。为了避免测试集信息泄漏，测试阶段的时序特征只允许使用测试集之前已经观测到的历史订单量。各模式通过配置文件统一管理，无需修改模型代码即可切换。

总体来看，集成学习模型与特征工程体系直接对应：时间周期特征、高峰标记、天气衍生特征、Geohash 编码和历史窗口特征均可直接进入模型训练。其主要优点是训练效率高、解释性较强、对表格特征适应性好，适合作为本实验的主要基线模型和可解释分析工具；不足是对空间邻接传播和长序列依赖的表达能力有限，需要依赖人工构造的滞后、滚动和交互特征补充时空信息。
